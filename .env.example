# Backend base URL (no trailing slash)
# Local Laravel example:
VITE_API_BASE_URL=http://localhost:8000/api/v1

# For staging/production builds, Vite injects env vars at build time.
# Create `.env.staging` / `.env.production` if you use `--mode staging` / production builds.
